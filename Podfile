# Uncomment the next line to define a global platform for your project
platform :ios, '11.2'

target 'StreetView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StreetView
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'ObjectMapper'
  pod 'SwiftyJSON'
  pod 'LGButton'
  pod 'Charts'
end
